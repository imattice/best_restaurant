<html>
    <head>
        <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css'>
        <title>Edit this cuisine</title>
    </head>
    <body>
        <div class="container">

        <h1>Update the {{ cuisines.getName }} category </h1>

        <form action="/cuisines/{{ cuisines.getId }}" method="post">
            <input name="_method" type="hidden" value="patch">
                <div class="form-group">
                    <label for="name">Rename your cuisine:</label>
                    <input id="name" name="name" type="text">
                </div>

          <button type="submit">Update!</button>
        </form>

        <form action="/cuisines/{{ cuisines.getId }}" method="post">
            <input name="_method" type="hidden" value="delete">

            <button type="submit">Delete this cuisine</button>
        </form>
    </body>
</html>
